JWT_SECRET=change-this-to-a-long-random-string
PORT=3002
# Path where data.json (accounts + login activity) is stored. In production,
# point this at a mounted persistent volume — without one, all accounts are
# lost on every container restart, not just redeploys. Defaults to the app
# directory (fine for local dev).
DATA_DIR=/data
